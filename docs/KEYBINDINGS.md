# Keybinding Reference

> Auto-generated from layer declarations. Edit keybindings in their source
> layers (`layers/base.py`, `layers/behavior.py`, `layers/user.py`).
> Do not edit this file manually.

---

## How Keybindings Work

Keybindings are declared as `(key, command, mode)` tuples in each layer's
`_keybindings()` method. During apply, the `LayerStack` merges them by
priority — higher-priority layers win on key conflicts.

```
BaseLayer[10] → BehaviorLayer[40] → PrivacyLayer[20] → UserLayer[90]
                                                          ↑ always wins
```

To override any binding, add it to `layers/user.py`.
To see all bindings in the running browser: `:bind`

---

## Normal Mode

### Navigation

| Key        | Command              | Layer    |
| ---------- | -------------------- | -------- |
| `gg`       | `scroll-to-perc 0`   | base     |
| `G`        | `scroll-to-perc`     | base     |
| `<ctrl-d>` | `scroll-page 0 0.5`  | base     |
| `<ctrl-u>` | `scroll-page 0 -0.5` | base     |
| `H`        | `back`               | behavior |
| `L`        | `forward`            | behavior |
| `r`        | `reload`             | behavior |
| `R`        | `reload -f`          | behavior |

### Tabs

| Key          | Command             | Layer    |
| ------------ | ------------------- | -------- |
| `J`          | `tab-prev`          | behavior |
| `K`          | `tab-next`          | behavior |
| `gt`         | `tab-next`          | base     |
| `gT`         | `tab-prev`          | base     |
| `d`          | `tab-close`         | base     |
| `u`          | `undo`              | base     |
| `co`         | `tab-only`          | base     |
| `tD`         | `tab-only --prev`   | behavior |
| `th`         | `tab-move -`        | behavior |
| `tl`         | `tab-move +`        | behavior |
| `tp`         | `tab-pin`           | behavior |
| `tm`         | `tab-mute`          | behavior |
| `<alt-1..9>` | `tab-focus 1..last` | behavior |

### Open / Navigate

| Key  | Command                       | Layer |
| ---- | ----------------------------- | ----- |
| `o`  | `cmd-set-text :open `         | base  |
| `O`  | `cmd-set-text :open -t `      | base  |
| `go` | `cmd-set-text :open {url}`    | base  |
| `gO` | `cmd-set-text :open -t {url}` | base  |

### Hints

| Key  | Command                     | Layer    |
| ---- | --------------------------- | -------- |
| `f`  | `hint`                      | behavior |
| `F`  | `hint all tab`              | behavior |
| `;d` | `hint links download`       | behavior |
| `;f` | `hint all tab-fg`           | behavior |
| `;b` | `hint all tab-bg`           | behavior |
| `;y` | `hint links yank`           | behavior |
| `;Y` | `hint links yank-primary`   | behavior |
| `;r` | `hint --rapid links tab-bg` | behavior |
| `;i` | `hint images`               | behavior |
| `;I` | `hint images tab`           | behavior |
| `;o` | `hint inputs`               | behavior |

### Search

| Key | Command          | Layer    |
| --- | ---------------- | -------- |
| `/` | `cmd-set-text /` | behavior |
| `?` | `cmd-set-text ?` | behavior |
| `n` | `search-next`    | behavior |
| `N` | `search-prev`    | behavior |

### Yank / Clipboard

| Key  | Command      | Layer    |
| ---- | ------------ | -------- |
| `yy` | `yank`       | base     |
| `yt` | `yank title` | base     |
| `,y` | `yank`       | behavior |
| `,Y` | `yank -s`    | behavior |

### Quickmarks & Bookmarks

| Key | Command                            | Layer    |
| --- | ---------------------------------- | -------- |
| `m` | `quickmark-save`                   | behavior |
| `'` | `cmd-set-text :quickmark-load `    | behavior |
| `"` | `cmd-set-text :quickmark-load -t ` | behavior |
| `B` | `cmd-set-text :bookmark-load `     | behavior |

### Download

| Key  | Command                      | Layer    |
| ---- | ---------------------------- | -------- |
| `gd` | `download`                   | behavior |
| `gD` | `download --dest ~/Desktop/` | behavior |

### Caret Mode Entry

| Key | Command                                       | Layer    |
| --- | --------------------------------------------- | -------- |
| `v` | `mode-enter caret`                            | behavior |
| `V` | `mode-enter caret ;; selection-toggle --line` | behavior |

### Pass-through

| Key        | Command                  | Layer |
| ---------- | ------------------------ | ----- |
| `<ctrl-v>` | `mode-enter passthrough` | base  |

---

## Leader Key Actions (`,` by default)

> Change `LEADER_KEY = ","` in `config.py` to remap all of these at once.

| Key  | Command                                   | Layer    |
| ---- | ----------------------------------------- | -------- |
| `,r` | `config-source`                           | behavior |
| `,e` | `config-edit`                             | behavior |
| `,t` | `cmd-set-text :set tabs.position `        | behavior |
| `,p` | `open -p`                                 | behavior |
| `,P` | `open -t -- {primary}`                    | behavior |
| `,y` | `yank`                                    | behavior |
| `,Y` | `yank -s`                                 | behavior |
| `,w` | `window-only`                             | behavior |
| `,d` | `download-clear`                          | behavior |
| `,D` | `download-delete`                         | behavior |
| `,x` | `tab-close`                               | behavior |
| `,X` | `undo`                                    | behavior |
| `,q` | `quit`                                    | behavior |
| `,Q` | `quit --save`                             | behavior |
| `,j` | `config-cycle content.javascript.enabled` | privacy  |
| `,i` | `config-cycle content.blocking.enabled`   | privacy  |
| `,c` | `config-cycle content.cookies.accept …`   | privacy  |
| `,R` | userscript readability                    | behavior |
| `,p` | userscript pass fill                      | behavior |
| `,P` | userscript pass OTP                       | behavior |

### Context Switching (`,C` prefix)

| Key    | Context  | Description                                   | Layer   |
| ------ | -------- | --------------------------------------------- | ------- |
| `,Cw`  | work     | Corporate tools, Jira, Google search          | context |
| `,Cr`  | research | arXiv, Scholar, Wikipedia, distraction-free   | context |
| `,Cm`  | media    | YouTube, Bilibili, Twitch; autoplay ON        | context |
| `,Cd`  | dev      | GitHub, MDN, crates, npm, DevDocs             | context |
| `,Cwt` | writing  | Dict, Thesaurus, Grammarly, focus mode        | context |
| `,Cg`  | gaming   | Steam, Twitch, ProtonDB, fullscreen ON _(v8)_ | context |
| `,C0`  | default  | Reset to base settings                        | context |
| `,Ci`  | —        | Show current context in message bar           | context |

---

## Insert Mode

| Key        | Command       | Layer    |
| ---------- | ------------- | -------- |
| `<Escape>` | `mode-leave`  | behavior |
| `<ctrl-e>` | `open-editor` | behavior |

---

## Caret Mode

| Key        | Command             | Layer    |
| ---------- | ------------------- | -------- |
| `<Escape>` | `mode-leave`        | behavior |
| `y`        | `yank selection`    | behavior |
| `Y`        | `yank selection -s` | behavior |

---

## Command Mode

| Key        | Command                      | Layer    |
| ---------- | ---------------------------- | -------- |
| `<Escape>` | `mode-enter normal`          | base     |
| `<ctrl-p>` | `completion-item-focus prev` | behavior |
| `<ctrl-n>` | `completion-item-focus next` | behavior |
| `<ctrl-j>` | `completion-item-focus next` | behavior |
| `<ctrl-k>` | `completion-item-focus prev` | behavior |

---

## Aliases

| Alias | Command                                | Layer |
| ----- | -------------------------------------- | ----- |
| `q`   | `quit`                                 | base  |
| `qa`  | `quit --save`                          | base  |
| `w`   | `session-save`                         | base  |
| `wq`  | `quit --save`                          | base  |
| `gh`  | `open -t https://github.com/redskaber` | user  |

---

## Customising Keybindings

Add to `layers/user.py`:

```python
def _keybindings(self) -> List[Tuple[str, str, str]]:
    L = self._leader
    return [
        # Open clipboard URL in new tab
        ("gx", "open -t -- {clipboard}", "normal"),
        # Copy as Markdown link
        (f"{L}M", "yank inline [{title}]({url})", "normal"),
        # Quick search selection
        ("ss", "hint --rapid links tab-bg", "normal"),
    ]
```

Changes take effect on `,r` (`:config-source`).

---

## Keyhint Dialog (Leader-Key Popup)

When you press the leader key (`,`) or any other multi-key prefix, qutebrowser
shows a **keyhint dialog** listing all completions for that prefix.

### Settings that control it

| Setting                 | Value   | Effect                                                                       |
| ----------------------- | ------- | ---------------------------------------------------------------------------- |
| `keyhint.delay`         | 200 ms  | Dialog appears 200 ms after the prefix key is pressed                        |
| `keyhint.radius`        | 6 px    | Rounded corners on the popup                                                 |
| `input.partial_timeout` | 3000 ms | Key sequence resets after 3 s of inactivity — **this is the critical value** |

### Why the hint used to flash and disappear

`input.partial_timeout` was set to `500 ms`. This meant:

1. You press `,` → keyhint delay (200 ms) starts
2. Dialog appears after 200 ms
3. `partial_timeout` fires at 500 ms — resets the key sequence
4. qutebrowser dismisses the keyhint dialog

**Fix (v10):** `input.partial_timeout` raised to `3000 ms`. You now have
3 seconds to press the second key after the hint appears. `Escape` always
cancels immediately.

### Blacklisting noisy prefixes

To suppress the hint for a prefix (e.g. `g` which has many bindings):

```python
# in layers/user.py _settings()
"keyhint.blacklist": ["g", "z"],
```

---

## Statusbar Clock

The clock widget is enabled by including `"clock"` in `statusbar.widgets`.

```python
"statusbar.widgets": ["keypress", "url", "scroll", "history", "progress", "clock"]
```

`"clock"` must be the **last** entry — it renders right-aligned at the far
right of the statusbar. Placing it before `"progress"` can cause it to
not appear in some qutebrowser builds.

The `"history"` widget (back/forward arrows) was added in v10 between
`"scroll"` and `"progress"`.

---

## Conflict Detection

Run from the project root:

```bash
python3 -c "
from keybindings.catalog import KeybindingCatalog
from layers.base import BaseLayer
from layers.behavior import BehaviorLayer
from layers.privacy import PrivacyLayer, PrivacyProfile
from layers.user import UserLayer

catalog = KeybindingCatalog.from_layers([
    BaseLayer(), BehaviorLayer(), PrivacyLayer(), UserLayer()
])
print(catalog.conflict_report())
"
```
